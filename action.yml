name: 'Hello World'
description: 'Greet someone'

runs:
  using: "composite"
  steps:
    - run: echo Hello World.
      shell: bash
    - run: echo "${{ github.action_path }}" >> $GITHUB_PATH
      shell: bash      
    - run: goodbye.sh
      shell: bash